// remove last element from array and return that element
// it modifies the original array

const numbers = [1,2,3,4,5];

const element = numbers.pop();

console.log("removed element : ", element); // 5

console.log(numbers); // [1,2,3,4]